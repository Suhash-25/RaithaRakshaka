# Leave empty when frontend and backend are served behind the same domain/proxy.
VITE_API_URL=

# For split deployments, set this to the public backend origin, for example:
# VITE_API_URL=https://api.your-domain.com
